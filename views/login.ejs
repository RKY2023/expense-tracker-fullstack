<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</head>
<body class="bg-dark">
    <div class="container w-50 mt-5">
        <div class="p-5 container bg-white border border-warning rounded">
            <info><% if (error) { %> <%= error.message%> <% } %></info>
            <h1 class="text-center"><% if (mode == 'signup') { %>Sign Up<% } else { %>Login<% } %></h1>
            <form onsubmit="getLogin()">
                <% if (mode == 'signup') { %> 
                <div class="form-group">
                    <label for="imputUsername">Username</label>
                    <input id="inputUsername" class="form-control" type="name" name="name" required>    
                </div>
                <% } %>
                <div class="form-group">
                    <label for="imputEmail">Email Address</label>
                    <input id="inputEmail" class="form-control" type="email" name="email" required>    
                </div>
                <div class="form-group">
                    <label for="imputPassword">Password</label>
                    <input id="inputPassword" class="form-control" type="password" name="password" required>    
                </div>
                <input type="hidden" name="mode" value="<%= mode %>">    
                <button class="btn btn-success w-100 mt-4 ">
                    <% if (mode == 'signup') { %>Signup<% } else { %>Login<% } %>
                </button>
            </form>
            <hr class="m-3">
            <div>
                <form action="<% if (mode == 'signup') { %>/user/login<% } else { %>/user/signup<% } %>" method="post">
                <input type="hidden" name="mode" value="<%= mode %>">    
                <button class="btn btn-primary w-100">
                    <% if (mode == 'signup') { %>Already have an Account? Login<% } else { %>Don't have an account? Signup<% } %>
                </button>
                </form>
            </div>
            <% if (userData) { %> 
            <div>
                <% if (userData.error) { %> 
                    <%= userData.error %>
                <% } %>
            </div>
            <% } %>
        </div>
        
        
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.0/axios.min.js"></script>
    <script src="/main.js"></script>
</body>

</html>